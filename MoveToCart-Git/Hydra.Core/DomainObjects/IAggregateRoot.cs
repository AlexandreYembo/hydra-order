namespace Hydra.Core.DomainObjects
{
    //Interface that represents the aggregation of my repository
    // 1 repository for each aggregation
    // each aggregation could contains many entities with relationship from each other.
    public interface IAggregateRoot
    {
         
    }
}